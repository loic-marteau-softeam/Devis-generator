<!doctype html>
<html lang="fr">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %> - Devis Generator</title>
    <link rel="stylesheet" href="/styles.css" />
  </head>
  <body>
    <header class="app-header">
      <div>
        <a href="/" class="brand">Devis Generator</a>
      </div>
      <% if (currentUser) { %>
      <nav>
        <a href="/clients">Clients</a>
        <a href="/quotes">Devis</a>
      </nav>
      <form action="/logout" method="post">
        <button type="submit">Déconnexion (<%= currentUser.username %>)</button>
      </form>
      <% } %>
    </header>
    <main class="container">
